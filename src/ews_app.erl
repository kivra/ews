%%  ews_app
%%
%% >-----------------------------------------------------------------------< %%

-module(ews_app).

-export([start/2, prep_stop/1, stop/1]).

-behaviour(application).

%% >-----------------------------------------------------------------------< %%

start(_, _) ->
    ews_sup:start_link().

prep_stop(State) ->
    State.

stop(_) ->
    ok.

%% >-----------------------------------------------------------------------< %%
